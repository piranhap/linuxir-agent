# Secrets — never commit a real .env (.env.template is the safe, committed stub)
.env
.env.*
!.env.template

# Investigation output workspaces
/out/

# Python / uv
__pycache__/
*.pyc
.venv/
.pytest_cache/
*.egg-info/
